package com.example.repasojueves

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.widget.Toast
import com.example.repasojueves.databinding.ActivityRecuperarContraseBinding

class RecuperarContrase : AppCompatActivity() {
   // private lateinit var binding: ActivityRecuperarContraseBinding
    //private lateinit var firebaseAuth:FirebaseAuth
    //override fun onCreate(savedInstanceState: Bundle?) {
        //binding=ActivityRecuperarContraseBinding.inflate(layoutInflater)
        //super.onCreate(savedInstanceState)
        //setContentView(binding.root)
        //firebaseAuth=Firebase.auth
        //binding.editTextTextEmailAddress.setOnClickListener{
            //recuperarcontraseña(binding.editTextTextEmailAddress.text.toString())
        }

    //}
    //private fun recuperarcontraseña(email:String){
       // firebaseAuth.sendPasswordResetEmail(email).addOnCompleteListener(){
        //        task->
           // if (task.isSuccesful){
             //   Toast.makeText(this,"El correo de recuperacion fue enviado" ,Toast.LENGTH_LONG).show()
           // }
            //else{
              //  Toast.makeText(this, "No fue posible enviar el correo" ,Toast.LENGTH_LONG).show()
            //}
       // }
  //  }
//}